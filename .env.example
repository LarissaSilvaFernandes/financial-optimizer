# Chave da Anthropic (obrigatoria) — console.anthropic.com -> API Keys
ANTHROPIC_API_KEY=

# Banco de dados PostgreSQL (usado apenas em producao / Docker)
DATABASE_PASSWORD=

# JWT secret — minimo 256 bits (32 caracteres)
JWT_SECRET=
